Light build (minify) for WTD Symposium landing

Este README foi movido para `docs/` para manter documentação histórica fora do bundle de deploy.

O conteúdo original descreve o script de build mínimo que gera `dist/wtd-symposium-landing.html`.

Para gerar o build:

```bash
cd /Users/appreciart/Desktop/WTD/wtd-symposium
npm install
npm run build
```

Nota: este README foi copiado para `docs/` — a versão original no root permanece (não removi), para evitar perda de histórico automática.
